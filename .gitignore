# Dependencies
node_modules/
*/node_modules/

# Logs
logs
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# Distribution / packaging
dist/
build/
.DS_Store
Thumbs.db

# (Removed) TriChatTutorApp was previously excluded because it was a nested git repo
# Allow it to be tracked now so the app files are committed